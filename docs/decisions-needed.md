# Decisions still required

The implemented MVP defaults are recorded in ADR 0004. The following release-level choices remain open:

- Select an OSS license and add the corresponding `LICENSE` file.
- Select and pin the production JVM base image by digest for releases.
- Define the startup benchmark environment and release threshold.
